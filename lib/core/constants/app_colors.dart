import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const background    = Color(0xFFF7F8FC);
  static const surface       = Color(0xFFFFFFFF);
  static const border        = Color(0xFFE8ECF4);
  static const inputFill     = Color(0xFFF0F2F8);

  // Brand
  static const primary       = Color(0xFF6C63FF); // Indigo-Violet
  static const primaryLight  = Color(0xFFEEEDFF);

  // Module colors — each module has its own identity
  static const academicHub   = Color(0xFF00C2A8); // Teal
  static const results       = Color(0xFFFF6B6B); // Coral
  static const internships   = Color(0xFFFFB020); // Amber
  static const projects      = Color(0xFF2ECC71); // Green
  static const compExams     = Color(0xFF3B82F6); // Sky Blue
  static const notifications = Color(0xFF9B59B6); // Purple
  static const certificates  = Color(0xFFE67E22); // Orange

  // Status
  static const success       = Color(0xFF27AE60);
  static const warning       = Color(0xFFF39C12);
  static const error         = Color(0xFFE74C3C);
  static const info          = Color(0xFF3B82F6);

  // Text
  static const textPrimary   = Color(0xFF1A1D3B);
  static const textSecondary = Color(0xFF6B7280);
  static const textLight     = Color(0xFF9CA3AF);
  static const textWhite     = Color(0xFFFFFFFF);
}
